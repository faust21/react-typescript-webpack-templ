import { createUseStyles } from 'react-jss';

const styles = createUseStyles({
  title: {
    textAlign: 'center',
    fontSize: 30,
    lineHeight: 2,
    fontWeight: 'bold',
  },
});

export default styles;
